export function getRealCandidatesByStateAndPosition(
  state: string,
  position: 'governador' | 'senador' | 'presidente'
): Array<{ id: string; name: string; party?: string; searchingPercentage?: number }> {
  // Stub: return empty array for MVP
  return [];
}
